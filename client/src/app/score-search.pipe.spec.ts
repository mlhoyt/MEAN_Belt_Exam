import { ScoreSearchPipe } from './score-search.pipe';

describe('ScoreSearchPipe', () => {
  it('create an instance', () => {
    const pipe = new ScoreSearchPipe();
    expect(pipe).toBeTruthy();
  });
});
